const Thought = require('../models/Thought');
const User = require('../models/User');

// Get all thoughts

// Get thought by id

// Create thought

// Update thought

// Delete thought

// Add reaction

// Remove reaction

module.exports = {
  getAllThoughts,
  getThoughtById,
  createThought,
  updateThought,
  deleteThought,
  addReaction,
  removeReaction,
};
